// eslint-disable-next-line import/prefer-default-export
export enum Page {
  HOME = '/',
  WRITE = '/write',
  SIGN_IN = '/signin',
  SIGN_UP = '/signup',
  POST = '/post',
  TEMPORARIES = '/temporaries',
}
